const batteryBatches = [4, 5, 3, 4, 4, 6, 5];

// Code your solution here
function batteries(batterBatches) { 
  let totalBatteries = 0; 
  batteryBatches.forEach(batterBatch => { 
    totalBatteries += batteryBatches;
  });
  return totalBatteries
}